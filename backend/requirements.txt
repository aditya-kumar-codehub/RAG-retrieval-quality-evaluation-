fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.9
slowapi>=0.1.9
python-dotenv>=1.0

# The core rag_eval library (retrievers, generation, judge) — installed as a
# regular (non-editable) package from the parent project directory. Run
# `pip install -r requirements.txt` from *inside* backend/ so this relative
# path resolves correctly.
../
