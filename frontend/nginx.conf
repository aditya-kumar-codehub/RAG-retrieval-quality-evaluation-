server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # SPA fallback so client-side routes (e.g. /analytics) resolve on refresh
    location / {
        try_files $uri $uri/ /index.html;
    }

    location /assets/ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
}
